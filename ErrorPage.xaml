<Page
    x:Class="BakedPotato.ErrorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BakedPotato"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="errBlock" Grid.Row="1" Grid.Column="1" Text="" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
        <Button x:Name="okButton" Grid.Row="2" Grid.Column="1" Click="okButton_Click" Width="200" Height="45" Content="OK" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0, 15, 0, 0"/>

    </Grid>
</Page>